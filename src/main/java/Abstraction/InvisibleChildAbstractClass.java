package Abstraction;

public abstract class InvisibleChildAbstractClass extends Animal{

    // this child class is now abstract and does not need to become a concrete child class.
    // this happens by adding abstract above and the class is not complaining.
}
